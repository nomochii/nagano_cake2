<div class="admin_item_show_title">
  <h1>商品詳細</h1>
</div>
<div class="container">
  <div class="row">
    <div class="col-6">
      <div class="admin_item_show_img">
        <% if @item.image.attached? %>
          <%= image_tag @item.image, size: "300x300" %>
        <% else %>
          <%= image_tag 'no_image', size: "300x300" %>
        <% end %>
      </div>
    </div>
    <div class="col-6">
      <table>
        <tr><th width="100" height="60">商品名</th><td><%= @item.product_name %></td></tr>
        <tr><th width="100" height="60">商品説明</th><td><%= @item.introduction %></td></tr>
        <tr><th width="100" height="60">税抜価格<p>（税込価格）</p></th><td><%= @item.price %>（<%= @item.add_tax_price.to_s(:dalimited) %>）円</td></tr>
      </table>
    </div>
  </div>
</div>
<div class="admin_item_edit_btn">
  <%= link_to "編集する", edit_admin_item_path(@item.id), class: 'btn btn-primary' %>
</div>