<header>
  <li>
    <%= link_to "商品一覧", admin_items_path %>
  </li>
  <li>
    <%= link_to "会員一覧", customers_path %>
  </li>
  <!--<li>-->
  <!--  <%#= link_to "注文履歴一覧", customers_path %>-->
  <!--</li>-->
  <li>
    <%= link_to "ログアウト", destroy_admin_session_path, method: :delete %>
  </li>
</header>

<h1>注文履歴一覧</h1>
  <table>
    <thead>
      <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
      </tr>
    </thead>

    <tbody>
      <% @customers.each do |customer| %>
        <tr>
         <td><%= link_to order.created_at.strftime('%Y/%m/%d'), admin_order_path(customer.id) %></td>
          <td>
            <%= customer.full_name, admin_customer_path(customer.id) %>
          </td>
          <td><%= customer.amount %></td>
        </tr>
      <% end %>
    </tbody>
  </table>